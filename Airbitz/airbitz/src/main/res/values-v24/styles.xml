<resources>
    <style name="AppTheme.Base" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Override alert style in order to correct marshmallow textColor -->
        <item name="alertDialogTheme">@style/DialogOverride</item>
        <item name="android:alertDialogTheme">@style/DialogOverride</item>
        <item name="android:timePickerDialogTheme">@style/DialogOverride</item>
        <item name="android:datePickerDialogTheme">@style/DialogOverride</item>
    </style>

    <style name="DialogOverride" parent="Theme.AppCompat.Light.Dialog">
        <item name="android:windowBackground">@android:color/white</item>
    </style>
</resources>
